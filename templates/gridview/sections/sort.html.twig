{# Back-compat alias: the sort block was renamed {sort} → {sortBar} for symmetry
   with {filterBar}. Existing layouts referencing {sort} keep working. #}
{% include '@FedaleGridview/gridview/sections/sortBar.html.twig' %}
