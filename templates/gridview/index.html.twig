{% extends 'base.html.twig' %}

{% block title %}{{ 'grid.index_title'|trans({}, 'GridviewBundle') }}{% endblock %}

{% block body %}
{# The grid's own {header}/{heading} renders the title (options.display.title);
   no separate page <h1> here, to avoid a duplicate heading. #}
{% include '@FedaleGridview/gridview/_grid.html.twig'
with {'gridview': gridview, 'columns': columns, 'models': models,
'pagination': pagination, 'form': form is defined ? form : null} %}
{% endblock %}